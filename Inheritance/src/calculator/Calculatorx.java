/*
 * Calculator 클래스가 final 클래스이므로 상속을 할 수 없다.
 * 그러므로 클래스 Calculatorx는 정의할 수 없다.
 */
package calculator;

/*
public final class Calculatorx extends Calculator {
	public Calculatorx(int sum) {
		super(sum);
	}
	
	public static void main(String[] args) {
		Calculator calc = new Calculator(0);
		calc.compute("+", 10);
		calc.compute("*", 10);
		calc.compute("-", 50);
		calc.compute("/", 25);
		calc.printSum();
	}

}
*/